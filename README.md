## chhajedji's build on vanilla emacs.

[init.el](./init.el) is just for initializing and doing the basic setup. Check [config.org](./config.org) for detailed build. `init.el` mainly sources `config.org`.
